# Function using def (normal function)
# def square(n):
#     return n * n

# Lambda function to calculate square of a number
square = lambda x: x * x

# Calling the lambda function and printing the result
print(square(4))



#lambda with multiple arguments
sum = lambda a,b,c : a+b+c
print(sum(2,5,3))



'''
    Imp Note about lambda function
    1. lambda is a short way to write a function
    2. x is the input number
    3. x * x is the square of the number
    4. square(4) returns 16
'''